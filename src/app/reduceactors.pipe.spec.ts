import { ReduceactorsPipe } from './reduceactors.pipe';

describe('ReduceactorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceactorsPipe();
    expect(pipe).toBeTruthy();
  });
});
